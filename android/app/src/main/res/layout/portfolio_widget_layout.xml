<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical">

    <!-- App label -->
    <TextView
        android:id="@+id/widget_app_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="KashU"
        android:textSize="11sp"
        android:textColor="#99FFFFFF"
        android:letterSpacing="0.08"
        android:fontFamily="sans-serif-medium" />

    <Space android:layout_height="4dp" android:layout_width="match_parent" />

    <!-- Portfolio value -->
    <TextView
        android:id="@+id/widget_portfolio_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="—"
        android:textSize="22sp"
        android:textColor="#FFFFFF"
        android:fontFamily="sans-serif-medium"
        android:maxLines="1"
        android:ellipsize="end" />

    <Space android:layout_height="2dp" android:layout_width="match_parent" />

    <!-- Gain / loss row -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_gain_loss"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="—"
            android:textSize="13sp"
            android:textColor="#4CAF50"
            android:fontFamily="sans-serif" />

        <TextView
            android:id="@+id/widget_gain_loss_pct"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="12sp"
            android:textColor="#99FFFFFF"
            android:layout_marginStart="6dp" />

    </LinearLayout>

    <Space android:layout_height="6dp" android:layout_width="match_parent" />

    <!-- Last updated -->
    <TextView
        android:id="@+id/widget_updated_at"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap to refresh"
        android:textSize="10sp"
        android:textColor="#66FFFFFF" />

</LinearLayout>
